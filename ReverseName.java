/**
  Reverse the first and last name in a given name.
  @param name a string with a first and last name, separated by a space
  @return the last name, a comma and space, then the first name.
  for example, change "Harry Smith" into "Smith, Harry"
*/

import java.util.*;

public class ReverseName {
  public static void main(String[] args){

    Scanner in = new Scanner(System.in);
    System.out.println("enter your name");
    String name = in.nextLine();
    String[] givenName = name.split(" ");
    for (int i = givenName.length; i > 0 ; i--){
      System.ou.println(givenName[])
  }
}
